include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # Disallow importing files from lib/ using relative paths. Require package: imports instead.
    avoid_relative_lib_imports: true

    # Common style and best-practice rules (recommended)
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_is_empty: true
    prefer_single_quotes: true
    avoid_print: true

    # Type and API usage
    always_declare_return_types: true
    always_specify_types: false
    annotate_overrides: true
    avoid_dynamic_calls: true
    avoid_redundant_argument_values: true
    unnecessary_brace_in_string_interps: true
    unnecessary_null_in_if_null_operators: true
    unnecessary_overrides: true

    # Flutter-specific useful rules
    no_logic_in_create_state: true
    avoid_web_libraries_in_flutter: true
    use_key_in_widget_constructors: true
    sort_constructors_first: true

analyzer:
  errors:
    # Treat the lint as an error so CI and `dart analyze` fail on violations.
    avoid_relative_lib_imports: error
